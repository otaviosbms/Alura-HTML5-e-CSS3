const nomes = ["ana Julia", "Caio vinicius", "BIA silva"];

const nomesPadronizados = nomes.map((nome) => nome.toUpperCase()); // to uppercase() -> tudo maiusculo.

console.log(nomesPadronizados);